const drink = {
  color: 'green',
  carbonated: true,
  sugar: 40,
};

type Drink = [string, boolean, number];
const pepsi: Drink = ['green', true, 40];

const carSpec: [number, number] = [400, 3352];

const carStast = {
  horsepower: 400,
  weight: 3354,
};
